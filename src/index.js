import React from "react";
import { render } from "react-dom";

import App from "./App";

// Renderiza o componente <App /> na <div id="app"> da aplicação
render(<App />, document.getElementById("app"));
